Hello Playwright

> A discovery and exploration of <a href='https://playwright.dev/'>playwright</a>.

In case of missing *operating system dependencies*:
 ```Shell
 npx playwright install-deps
 ```

-----

Also <a href='https://www.npmjs.com/package/dotenv'>dotenv</a> has been used.
 ```Shell
npm i dotenv
 ```
